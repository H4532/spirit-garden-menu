(()=>{const STORAGE='spiritGardenMenuV1';const clone=v=>JSON.parse(JSON.stringify(v));const stored=localStorage.getItem(STORAGE);const data=stored?JSON.parse(stored):clone(window.SPIRIT_GARDEN_SEED);let lang=localStorage.getItem('spiritGardenLang')||'en';let query='';let active='all';let availableOnly=false;const $=s=>document.querySelector(s);const iconFor=id=>id.includes('dessert')?'🍰':id.includes('bakery')?'🥐':id.includes('sandwich')?'🥪':id.includes('refresh')?'🍹':id.includes('traditional')?'🫖':id.includes('drip')?'☕':'☕';function t(en,ar){return lang==='ar'?ar:en}function setLang(){document.documentElement.lang=lang;document.documentElement.dir=lang==='ar'?'rtl':'ltr';$('#languageBtn').textContent=lang==='ar'?'English':'العربية';$('#eyebrow').textContent=t('Digital Coffee Menu','قائمة القهوة الرقمية');$('#heroTitle').textContent=t(data.settings.brand,data.settings.brandAr);$('#heroText').textContent=t(data.settings.tagline,data.settings.taglineAr);$('#searchInput').placeholder=t('Search coffee, tea, dessert...','ابحث عن قهوة أو شاي أو حلوى...');$('#availabilityBtn').textContent=t(availableOnly?'Show all':'Available only',availableOnly?'عرض الكل':'المتوفر فقط');$('#footerText').textContent=t('Prices are in Saudi Riyals.','الأسعار بالريال السعودي.');$('#updatedText').textContent=t('Updated ','آخر تحديث ')+(data.settings.updatedAt||'');render()}function renderChips(){const chips=[{id:'all',name:t('All','الكل')}].concat(data.sections.filter(s=>s.active).sort((a,b)=>a.order-b.order).map(s=>({id:s.id,name:t(s.name,s.nameAr)})));$('#sectionChips').innerHTML=chips.map(c=>`<button class="chip ${active===c.id?'active':''}" data-id="${c.id}">${c.name}</button>`).join('');$('#sectionChips').querySelectorAll('button').forEach(b=>b.onclick=()=>{active=b.dataset.id;render()})}function filteredItems(sectionId){return data.items.filter(i=>i.sectionId===sectionId).filter(i=>!availableOnly||i.available).filter(i=>{const hay=`${i.name} ${i.nameAr} ${i.description||''} ${i.descriptionAr||''}`.toLowerCase();return hay.includes(query.toLowerCase())}).sort((a,b)=>(a.order||0)-(b.order||0))}function card(i){const desc=t(i.description||'',i.descriptionAr||'');return `<article class="card ${i.available?'':'sold'}">${i.available?'':`<span class="sold-label">${t('Sold out','نفد')}</span>`}<div class="card-top"><div class="product-icon">${iconFor(i.sectionId)}</div><div class="price">${i.price} ${t('SAR','ر.س')}</div></div><h3>${t(i.name,i.nameAr)}</h3><div class="arabic">${lang==='ar'?i.name:i.nameAr}</div>${desc?`<p class="description">${desc}</p>`:''}${i.badges?.length?`<div class="badges">${i.badges.map(b=>`<span class="badge ${b}">${b.replaceAll('-',' ')}</span>`).join('')}</div>`:''}<div class="meta">${Number.isFinite(i.calories)?`<span>🔥 ${i.calories} ${t('cal','سعرة')}</span>`:''}${i.time?`<span>⏱ ${i.time} ${t('sec','ث')}</span>`:''}</div></article>`}function render(){renderChips();const sections=data.sections.filter(s=>s.active).sort((a,b)=>a.order-b.order).filter(s=>active==='all'||s.id===active);let html='';sections.forEach(s=>{const items=filteredItems(s.id);if(items.length)html+=`<section class="section" id="${s.id}"><div class="section-head"><div><div class="section-sub">${lang==='ar'?s.name:s.nameAr}</div><h2>${t(s.name,s.nameAr)}</h2></div><div class="section-sub">${items.length} ${t('items','أصناف')}</div></div><div class="grid">${items.map(card).join('')}</div></section>`});$('#menuRoot').innerHTML=html||`<div class="empty">${t('No menu items match your search.','لا توجد أصناف مطابقة لبحثك.')}</div>`}$('#languageBtn').onclick=()=>{lang=lang==='en'?'ar':'en';localStorage.setItem('spiritGardenLang',lang);setLang()};$('#searchInput').oninput=e=>{query=e.target.value;render()};$('#availabilityBtn').onclick=()=>{availableOnly=!availableOnly;setLang()};setLang()})();